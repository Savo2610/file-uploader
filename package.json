{
  "name": "file-uploader",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "dev:download": "wrangler dev --port 8789 --host download.veerka.mp",
    "test": "node tools/test-api.mjs",
    "cleanup": "node tools/cleanup-tests.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
